#include <bits/stdc++.h>
using namespace std;

#define pb push_back
#define eb emplace_back
#define mp make_pair
#define pq priority_queue

using ll = long long;		// integral value type
using ff = long double;		// floating point type
using vl = vector<ll>;
using pl = pair<ll, ll>;
using vpl = vector<pl>;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout << fixed << setprecision(10);

	ll N; cin >> N;
}
